{
  "name": "visualsgifs",
  "displayName": "visualSgifs",
  "description": "Shows local GIFs as editor decorations based on events.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
  "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "visualsgifs.helloWorld",
        "title": "Test: Show a visualSgifs GIF"
      }
    ],
    "configuration": {
      "title": "visualSgifs Configuration",
      "properties": {
        "visualsgifs.gifFolderPath": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Absolute path to your main GIF folder. (e.g., C:\\Users\\YourName\\Documents\\MyGifs)"
        },
        "visualsgifs.activeSeries": {
          "type": "array",
          "default": [],
          "description": "List of series (sub-folders) to activate. (e.g., [\"k-on\", \"lain\"]) GIFs in the root folder are always active."
        },
        "visualsgifs.display.position": {
          "type": "string",
          "enum": [
            "bottom-right",
            "bottom-left",
            "top-right",
            "top-left"
          ],
          "default": "bottom-right",
          "description": "Corner of the editor to display the GIF."
        },
        "visualsgifs.display.maxWidth": {
          "type": "number",
          "default": 300,
          "description": "The maximum width (in pixels) of the GIF."
        },
        "visualsgifs.display.maxHeight": {
          "type": "number",
          "default": 300,
          "description": "The maximum height (in pixels) of the GIF."
        },
        "visualsgifs.display.durationSeconds": {
          "type": "number",
          "default": 5,
          "description": "How long the GIF stays on screen (in seconds). Set to 0 to keep it until the next event."
        },
        "visualsgifs.afk.timeInMinutes": {
          "type": "number",
          "default": 5,
          "description": "Time in minutes of inactivity to be considered AFK."
        },
        "visualsgifs.tags.error": {
          "type": "array",
          "default": [
            "angry",
            "cry",
            "pout",
            "error"
          ],
          "description": "Tags to search for when an error is detected. (e.g., 'cry_1.gif')"
        },
        "visualsgifs.tags.afk": {
          "type": "array",
          "default": [
            "sleeping",
            "bored",
            "afk"
          ],
          "description": "Tags to search for when you go AFK."
        },
        "visualsgifs.tags.test": {
          "type": "array",
          "default": [
            "happy",
            "pat",
            "wag",
            "test"
          ],
          "description": "Tags to search for with the 'Test GIF' command."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.46.3",
    "eslint": "^9.39.1",
    "esbuild": "^0.27.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
